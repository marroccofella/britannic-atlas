const fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'..');
function edit(file,before,after){const target=path.join(root,file),text=fs.readFileSync(target,'utf8');if(!text.includes(before))throw new Error('No match in '+file);fs.writeFileSync(target,text.replace(before,after));}
edit('oracle/tests/ui-source-contract.test.mjs','select, button, \\[contenteditable', 'select, button, a, summary, \\[contenteditable');
edit('app/manx/earth/page.tsx','<div className="kb-header-links">','<div className="kb-header-links"><Link className="kb-back" href="/manx/discover#explore=isle-of-man%2Fgeography-places">Explore island topics</Link>');
fs.appendFileSync(path.join(root,'oracle/README.md'),'\n## Island discovery explorer (10 September 2026)\n\nThe main navigation now connects Talk to Mani and Explore the island. Explore includes the complete 2,869-topic hierarchy, full-catalogue search, breadcrumbs, browser Back/Forward support, contextual related subjects and curated links to existing Manx research pages. Source-backed descriptions remain distinct from research outlines.\n\nBring question to Mani prepares an editable question in the current conversation. Browsing does not submit a request, switch conversations or cancel research. If a draft was already present, Restore my previous draft recovers it. Hosted explorer links open the local Mani app with a question in the URL fragment; they do not run the local answer engine in the cloud.\n\nThe canonical explorer assets are in `public/discovery/`. After editing them, run `node tools/sync-discovery.mjs` to refresh the static copy served by Mani. `node --test tests/discovery.test.mjs` verifies that both copies match, every topic is reachable, and curated routes exist.\n');
console.log('Return navigation and integration notes updated.');
